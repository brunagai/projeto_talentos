# Deve ser o mesmo valor de SECRET_KEY do backend (nunca use NEXT_PUBLIC_).
AUTH_SECRET=
# Opcional — nome do cookie de sessão (padrão: access_token)
# AUTH_COOKIE_NAME=access_token
# URL da API usada nos rewrites do Next
NEXT_PUBLIC_API_URL=http://localhost:8000
