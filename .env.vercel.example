# Variáveis no Vercel (projeto só do frontend — Root Directory = frontend)

AUTH_SECRET=mesmo_valor_do_SECRET_KEY_do_backend
COOKIE_SECURE=true
AUTH_COOKIE_NAME=access_token
# URL pública do backend (Railway/Render), SEM barra no final
NEXT_PUBLIC_API_URL=https://SEU-BACKEND.up.railway.app
